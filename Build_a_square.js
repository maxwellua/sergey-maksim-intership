function getShape(value) {
    let pluses = '';

    for (let iteration = 0; iteration < value; iteration++) {
        pluses += '+';
        }
    for (let iteration = 0; iteration < value; iteration++) {
        console.log(pluses)
    }
}
